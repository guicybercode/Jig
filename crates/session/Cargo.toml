[package]
name = "cli-master-session"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "PTY-backed session manager for CLI Master"

[dependencies]
cli-master-core = { path = "../core" }
nix = { version = "0.30.1", default-features = false, features = ["signal", "process"] }
portable-pty = "0.9.0"

[dev-dependencies]
cli-master-fake-agent = { path = "../fake-agent" }
tempfile = "3"

[lints]
workspace = true
