//! Cross-crate helpers for the Beta v0.1 acceptance flow.

/// Placeholder so the e2e package is a proper library crate.
#[must_use]
pub const fn crate_name() -> &'static str {
    "cli-master-e2e"
}
